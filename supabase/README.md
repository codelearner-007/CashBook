# Supabase Setup

1. Create project at supabase.com
2. Run `migrations/001_init.sql` in SQL Editor
3. Enable Google OAuth: Authentication → Providers → Google
4. Create Storage bucket named `attachments`
5. Copy URL + keys to frontend/.env and backend/.env
